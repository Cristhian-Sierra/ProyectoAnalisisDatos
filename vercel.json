{
    "version": 2,
    "routes": [
      { "src": "/predict", "dest": "main:app" },
      { "src": "/", "dest": "main:app" }
    ],
    "env": {
      "UVICORN_CMD": "uvicorn main:app --host 0.0.0.0 --port $PORT"
    }
  }
  